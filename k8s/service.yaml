apiVersion: v1
kind: Service
metadata:
  name: api-pasteleria-service
  namespace: default
  labels:
    app: api-pasteleria
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 5000
    protocol: TCP
  selector:
    app: api-pasteleria